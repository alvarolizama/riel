# references/ — local notes

Distilled evidence and private operational notes. Everything here is
**local-only** gitignored — the public repo keeps only the self-contained
skills and the system-agnostic specs.

## Contents

- `papers-and-sources.md` — the evidence base: papers, repos, and reports
  that shaped Riel's design (reference & inspiration, not a public promise).
- `*-adapter-notes.md` — operational detail of each remote adapter (private;
  the public contract lives in `specs/spec-adapters.md`). One file per
  adapter, named after the remote system.

## Evidence distillation

The full design logic lives in the personal notes, not here. The sources
actually used by each component are cited inline in the skills.

Add future private notes here (e.g. measurement logs, adapter notes) — keep
the public repo clean.
